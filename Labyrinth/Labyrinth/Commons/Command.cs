namespace Labyrinth.Commons
{
    /// <summary>
    /// Enumeration used for handling the user's input
    /// </summary>
    public enum Command
    {
        Restart,
        Up,
        Down,
        Left,
        Right,
        Top,
        Exit,
        InvalidInput,
    }
}